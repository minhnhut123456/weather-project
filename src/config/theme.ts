const fontFamily = "'Inter'";
const colorBlack = '#262834';
const colorGrey = '#707070';

export type BaseTheme = typeof theme;

export const theme = {
  color: {
    colorBlack,
    colorGrey,
  },
  typography: {
    fontFamily,
  },
};
